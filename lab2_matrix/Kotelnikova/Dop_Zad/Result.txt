Результаты (приблизительно - усредненные) умножений двух квадратных матриц размером 500 х 500:

1. Base Matrix (массив с ссылками на массивы) - 800 мс
2. Line Matrix (единый массив значений) - 1000 мс
3. Sparse Matrix (разреженная матрица, IndRow - индексы строк ненулевых элементов) - 9000 мс
4. CSR матрица (разреженная матрица, IndRow - начало строк ненулевых элементов) - 60 мс

Вывод: Самый эффективный способ представления матриц - CSR.

p.s. - я правда не понимаю почему Sparse Matrix самый долгий, я уже как только можно увеличивала количество нулей в матрице, но это не помогает, ошибки в реализации не вижу...